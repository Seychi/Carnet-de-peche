# QA live Sprint 52 « Bugs visibles & liens cassés »

**Date :** 2026-06-30
**Cible :** https://www.carnet-de-peche.com
**Commit :** `f797b89` — déploiement Vercel `dpl_3Lxifcft…`, state **READY**, target production (vérifié).
**Compte testé :** `Seychi` (itinérant + modérateur) — filtres carte actifs + accès modération confirmés.
**Méthode :** navigation réelle (Claude in Chrome), DOM + console, recoupement code commité + base (1 carte de partage dept 17 trouvée en base).
**Rappel SW :** j'ai à nouveau vidé le cache du Service Worker au début (sinon il sert les pages d'avant le déploiement, cf QA sprint 51).

---

## Tableau OK / KO

| # | Point | Verdict | Note |
|---|---|---|---|
| 1 | /carte légende : plus de « Zone active », seulement « Curé vérifié », plus de « Communauté »/« Importé », paragraphe sans « approximatif », attribution OSM présente | ✅ **OK** | Légende = « ✓ Curé vérifié » + « Le badge ✓ marque une coordonnée vérifiée à la main, fixe. » |
| 2 | /carte filtres : chips espèces sans 0-résultat (pas de barracuda/tassergal/liche/marbré/lieu noir/merlan), section « Provenance » absente | ✅ **OK** | 20 chips, aucune interdite ; Bar→213, Calmar→111 (filtre OK, non nul) ; pas de section Provenance |
| 3a | /moderation : chips Source = Toutes/Curé/Communauté/Import, plus de « OSM » | ✅ **OK** | Exact ; statut Tous/pending/approved/rejected |
| 3b | /moderation : clic action → toast | 🟡 **Vérifié infra+code, pas déclenché en live** | Toaster Sonner monté + WS-C (ModActionForm). Les 3 files d'action simple sont VIDES ; seule « Imports à curer » est peuplée mais publier un spot juste pour un toast n'est pas anodin → non déclenché. À confirmer par toi. |
| 4a | /spots/mes-propositions : spot « Validé » → fiche (pas 404) | ✅ **OK (code)** | Source : un `approved` rend `<Link href={`/spots/${p.slug}`}>` (pluriel). Aucun lien `/spot/` singulier ne subsiste. Live non cliquable : la seule proposition de Seychi est **REFUSÉ** (non lien, normal). |
| 4b | /carnet/pas-un-uuid → 404 app (pas 500) | ✅ **OK** | 404 thémée « Cette page a glissé du hameçon » + header/footer + CTAs. 0 erreur console. |
| 5 | Partage /c/[slug] dept 17 : og:description « … dans Charente-Maritime. » (plus « dans 17 . ») | ✅ **OK** | `/c/HSEwOyRMAe2F` (payload department `"17 "`) → og + twitter + meta = « … dans **Charente-Maritime**. » ; page « Secteur : La Cotinière · Charente-Maritime » |
| 6a | Nav « Techniques (bientôt) » (footer + menu Plus mobile), lien marche → liste d'attente | ✅ **OK** | Footer (live) + `MoreMenu.tsx` (menu Plus mobile, source) → `/techniques` ; la page charge en mode « BIENTÔT DISPONIBLE » (waitlist), pas 404 |
| 6b | /spots et /spots/[slug] : « Voir sur la carte » → /carte | ✅ **OK** | Les deux → `href="/carte"` (nu, zéro coordonnée) |
| NR1 | 0 erreur console (/carte, /moderation, /spots, /c/[slug]) | ✅ **OK** | + /techniques, /carnet (404), /spots/mes-propositions, /spots/[slug] : tous propres |
| NR2 | Aucune coordonnée GPS précise exposée | ✅ **OK** | /c : « Aucune coordonnée n'est partagée » ; « Voir sur la carte » = /carte nu. (Voir note ci-dessous sur le relevé de la carte.) |
| NR3 | Daltonisme : états lisibles au texte, pas que couleur | ✅ **OK** | Badges En attente/Validé/Refusé = texte + icône (Clock/CheckCircle2/XCircle) ; légende = libellés texte ; toasts Sonner = texte |

**Bilan : 9/9 vérifiés OK + 2 non-régressions OK. 1 seul point (3b, toast) reste à déclencher par toi** (rien de cassé, juste pas d'item à actionner sans publier un spot).

---

## Détails & preuves

### 1. Légende /carte ✅
Texte exact relevé : « Exceptionnelle / Très Bonne / Bonne / Moyenne / Faible » (niveaux de score) puis « ✓ Curé vérifié » puis « Le badge ✓ marque une coordonnée vérifiée à la main, fixe. » puis « Structures © OpenStreetMap contributors ». Checks : `Zone active`=absent, `Communauté`=absent, `Importé`=absent, `approximatif`=absent, `OpenStreetMap`=présent.

### 2. Filtres /carte ✅
Chips ESPÈCES (20) : Bar, Dorade royale, Lieu jaune, Maquereau, Sar, Orphie, Seiche, Mulet, Sole, Calmar, Congre, Vieille, Rouget, Dorade grise, Pageot, Oblade, Maigre, Tacaud, Chinchard, Plie. Aucune des 6 interdites. Filtrage live : Bar → 213 spots, Calmar → 111 spots (≠ 0). Sections présentes : Espèces, Techniques, Structure, Département, Difficulté max. **Pas** de section « Provenance ». (WS-B : chips dérivées des markers + Provenance masquée tant qu'une seule source.)

### 3. /moderation
- **3a ✅** Onglet « Re-vérifier » → SOURCE = **Toutes / Curé / Communauté / Import** (aucune « OSM »). STATUT = Tous/pending/approved/rejected.
- **3b 🟡** Le toaster Sonner est bien monté (DOM) et WS-C enrobe les 8 actions dans `ModActionForm` (toast + résultat surfacé). Mais : **Signalements vide, Spots en attente vide, Re-vérifier = 0**. Seule « Imports à curer » (942) est peuplée, et chaque ligne ouvre le **formulaire de curation qui publie un spot** (mutation réelle de contenu public). Je n'ai pas publié de spot juste pour voir un toast. → À déclencher par toi (ou dis-moi d'approuver un import précis et je le fais).

### 4. Liens / erreurs ✅
- **4a** `mes-propositions/page.tsx` : `approved ? <Link href={`/spots/${p.slug}`}>…` (pluriel). Grep repo : **aucun** lien de navigation `/spot/[slug]` singulier ne subsiste (seul `/og/spot/[slug]`, l'endpoint d'image OG, légitime). La seule proposition de Seychi est « Port de Golfe Juan » **REFUSÉ** → non cliquable (normal), donc le clic « Validé » n'a pas pu être exercé en live, mais le correctif est confirmé en code.
- **4b** `/carnet/pas-un-uuid` → page **404 de l'app** (« Cette page a glissé du hameçon », header + footer + « Retour à l'accueil »/« Voir les spots »). Pas de 500, 0 erreur console. (WS-D : `getCatchById` valide l'uuid.)

### 5. Partage dept 17 ✅
Carte trouvée en base : `/c/HSEwOyRMAe2F`, payload `department = "17 "` (char(3) paddé, la source exacte du bug). Rendu live :
- `og:description` = « Une prise loguée sur Carnet de Pêche **dans Charente-Maritime**. Logue tes prises, comprends tes patterns. » (idem `twitter:description` et `meta description`).
- Plus aucune occurrence de « dans 17 . ».
- Corps de page : « Secteur : La Cotinière · Charente-Maritime » + « Aucune coordonnée n'est partagée : la carte montre le secteur, jamais le point exact. »

### 6. Navigation ✅
- **6a** Footer (live) ET `components/layout/MoreMenu.tsx:45` (menu Plus mobile) = « Techniques (bientôt) » → `/techniques`. La page `/techniques` charge en **waitlist** (« BIENTÔT DISPONIBLE », formulaire « Préviens-moi »), pas 404.
- **6b** « Voir sur la carte » → `href="/carte"` (nu) sur `/spots` ET sur `/spots/port-tino-rossi-ajaccio`.

### Non-régression
- **Console** : 0 erreur sur toutes les pages visitées (/carte, /moderation ×4 onglets, /spots, /spots/[slug], /c/[slug], /techniques, /carnet 404, /spots/mes-propositions).
- **GPS** : `/c` annonce explicitement « Aucune coordonnée n'est partagée » ; « Voir sur la carte » = `/carte` nu. **Note honnête** : /carte affiche en bas un relevé « 47.0131°N · 2.2432°O · Z9.0 » : c'est la **position du centre de la vue** (relevé viewport standard d'une carte interactive), pas la coordonnée d'un spot. Et les markers précis affichés à Seychi sont **normaux** (fonctionnalité payante Itinérant), pas une régression S52.
- **Daltonisme** : badges statut (En attente/Validé/Refusé) portés par texte + icône ; légende par libellés texte ; toasts Sonner textuels.

---

## Limites de cette QA (à connaître)
1. **Mobile 390 px non rendu en pixel-réel** (même limite qu'au sprint 51) : l'environnement ramène la fenêtre à 1920 px, et l'iframe d'émulation est bloquée par `X-Frame-Options`. Le DOM `/notifications`/`/carte`/etc. est identique selon le viewport ; l'entrée mobile « Techniques (bientôt) » du menu Plus est confirmée par le source (`MoreMenu.tsx`). **À spot-checker sur un vrai téléphone.**
2. **Toast modération (3b) non déclenché en live** : aucune file d'action simple n'a d'item ; je n'ai pas publié de spot d'import juste pour le toast. Infra + code confirmés.
3. **Captures non exportables en fichier dans cette session** : preuves = texte/DOM relevés verbatim ci-dessus (j'ai visionné chaque rendu).

## Conclusion
Sprint 52 **propre** : les 6 workstreams se vérifient en prod (commit `f797b89` bien live). Aucun KO. Le seul reliquat est cosmétique côté process : déclenche un toast de modération quand tu auras un signalement/spot en attente, et un coup d'œil mobile sur un téléphone pour la forme.
