# Audit 360° Carnet de Pêche — jeudi 21 mai 2026

> Audit auto-généré par tâche planifiée, 20h (run effectif : 00h47 UTC du 21/05, soit 02h47 Paris — premier passage du rituel après mise en place).
> Référence fondatrice : `docs/AUDIT-2026-05.md` (20 mai 2026)
> Audit précédent : aucun — c'est le premier audit quotidien du dossier `docs/audits/`. Le baseline delta est donc l'audit fondateur du 20/05.
> Sprint en cours : **Sprint 7.5 — Polish marketing / SEO / infra** (3-5 jours, démarré dans le brief `docs/sprint-7.5/brief-sprint-7.5.md` du 20/05, **pas encore commencé en code**).

---

## 0. Synthèse exécutive

### Verdict en une ligne

**Tu as un brief sprint 7.5 béton sur le bureau depuis 24h, mais zéro ligne corrigée : tout ce que l'audit fondateur listait en 🚨 est encore là, et la page `/spots/pointe-du-raz` continue de partager un canonical `vercel.app` à chaque fetch Google ou Twitter.**

### État du jour en 5 phrases

1. **Aucun commit depuis le 20 mai 2026 19h35** — pas de travail livré sur le sprint 7.5 dans la fenêtre des dernières 24h, mais c'est cohérent avec le timing (brief reçu hier, démarrage demain).
2. **Site live opérationnel** : home + tarifs + spots + fiche spot + carte + guides + auth + pages légales accessibles. Pas de page down.
3. **Tous les P0 du 20/05 sont encore ouverts** : `metadataBase` toujours sur `vercel.app`, footer toujours cassé (`/fil`, `/especes`, `/techniques` retournent un body vide), toast "sprint 4" toujours dans `pricing-cards.tsx`, témoignages fictifs toujours en home, 4 `href="#"` toujours présents.
4. **Pas de fuite secret détectée** dans le git log ni dans le code source — bonne hygiène conservée.
5. **Un risque légal nouveau, jamais mentionné dans l'audit fondateur** : les pages `/legal/mentions-legales` et `/legal/confidentialite` contiennent **8 champs `[À COMPLÉTER]`** publiquement visibles (SIRET, dénomination, adresse, directeur de publication). Bloquant avant tout paiement Stripe.

### Alertes 🚨

🚨 **P0 NOUVEAU — Mentions légales incomplètes en prod**
Les pages `/legal/mentions-legales` et `/legal/confidentialite` affichent publiquement des `[À COMPLÉTER PAR JOHN — ex. : Carnet de Pêche SAS / EI John Dupont]`. Avant d'activer Stripe (sprint 9), c'est non négociable juridiquement. À traiter en même temps que le sprint 7.5 ou en bloc 0 du sprint 8.

🚨 **P0 PERSISTANT — `metadataBase` = vercel.app**
24e jour d'erreur SEO. Chaque crawl Googlebot enregistre des canonicals vers `carnet-de-peche.vercel.app`. Le `robots.txt` lui-même pointe son `Sitemap:` vers `vercel.app/sitemap.xml`. À fixer dès la première heure de sprint 7.5.

🚨 **P0 PERSISTANT — Footer cassé**
`/fil`, `/especes`, `/techniques` renvoient un body vide (404 implicite Next.js). Trois clics morts sur chaque page publique.

⚠️ **P1 PERSISTANT — Routes dev exposées en prod sans guard**
`app/test/page.tsx`, `app/dev/test-photo/page.tsx`, `app/api/dev-test/route.ts` n'ont aucun guard `NODE_ENV !== 'development' → notFound()`. Seules `/dev/scoring-preview` et `/dev/solunar-preview` ont été correctement protégées.

---

## 1. Delta depuis le dernier audit

### 1.1 Activité git (0 nouveau commit depuis 24h)

Le dernier commit `main` date du 20 mai 19h35 :
```
8c7200a 2026-05-20 chore: redeploy apres correction CRON_SECRET
97132cb 2026-05-20 chore: redeploy avec CRON_SECRET + SUPABASE_SERVICE_ROLE_KEY
33f2037 2026-05-20 fix(vercel): cron quotidien au lieu de horaire (limite plan Hobby)
044fc80 2026-05-20 refactor(scoring): profil descriptif honnete + fix cache profil
3aeb485 2026-05-20 feat(scoring): sprint 7 — UI insights perso, markers carte, cron scores
```

Aucun commit depuis. Le brief sprint 7.5 (`docs/sprint-7.5/brief-sprint-7.5.md`, 574 lignes) a été produit hier mais aucun bloc A/B/C/D n'a été attaqué côté code.

**Lecture** : tu as pris la journée pour digérer l'audit fondateur, ce qui est sain. Mais à partir d'aujourd'hui, chaque jour sans commit sur le sprint 7.5 est un jour où le site live continue de saboter ta SEO et ta crédibilité.

### 1.2 Changements observés sur le site live

Aucun. Le crawl du jour est strictement identique au snapshot du 20/05 décrit dans l'audit fondateur (sections 1.1 à 1.6) :

- Home : 4 `href="#"` toujours présents (lignes 244, 342, 384, 494 de `app/(marketing)/page.tsx`), témoignages Yann/Julien/François affichés, "217 spots actifs aujourd'hui" affiché, "rayon de 2 km" toujours là, "Import / export GPX/Fishbrain" toujours promis.
- Tarifs : toast "sprint 4" toujours déclenché par les 2 boutons "Essayer 7 jours" (`pricing-cards.tsx:67-71, 173, 218`).
- Fiche spot Pointe du Raz : canonical et OG image sur `vercel.app` (confirmé par `meta-og:url: https://carnet-de-peche.vercel.app/spots/pointe-du-raz` dans le crawl).
- Footer : 3 entrées toujours présentes (`components/layout/Footer.tsx:32-34`), aucune page créée pour les desservir.

### 1.3 Statut des P0/P1 de l'audit fondateur (`docs/AUDIT-2026-05.md` §5)

| Item | Source | Statut au 21/05 | Preuve |
|---|---|---|---|
| **P0.1** `metadataBase` → `.com` | Audit 5.1 | 🔴 ouvert | `app/layout.tsx:19` : `new URL('https://carnet-de-peche.vercel.app')` |
| **P0.2** Footer stubs ou suppression | Audit 5.1 | 🔴 ouvert | `Footer.tsx:32-34` inchangé, pages absentes |
| **P0.3** Pricing 7j ou 14j (décision) | Audit 5.1 | 🔴 ouvert | Home dit "14j" (page.tsx:257), /tarifs dit "7 jours satisfait ou remboursé" |
| **P0.4** Corriger `href="#"` + toast sprint 4 | Audit 5.1 | 🔴 ouvert | 4 `href="#"` confirmés + handleTrialClick inchangé |
| **P0.5** Aligner copy home avec réalité | Audit 5.1 | 🔴 ouvert | "Import/export", "Modération humaine ambassadeurs", "217 spots", "2 km" tous présents |
| **P1.1** Témoignages | Audit 5.2 | 🔴 ouvert | Yann L. / Julien R. / François B. toujours en home |
| **P1.2** Réconciliation migrations local/remote | Audit 5.2 | 🟡 non vérifiable depuis sandbox — migration 015 + 016 toujours dernières |
| **P1.3** `env.ts` zod : `CRON_SECRET` + `SERVICE_ROLE_KEY` | Audit 5.2 | 🔴 ouvert | `lib/env.ts` ne valide que `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_PROJECT_REF.optional()` |
| **P1.4** Setup CI GitHub Actions | Audit 5.2 | 🔴 ouvert | Pas de `.github/workflows/` |
| **P1.5** Retirer `eslint.ignoreDuringBuilds` | Audit 5.2 | 🔴 ouvert | `next.config.ts:5` : `eslint: { ignoreDuringBuilds: true }` |
| **P1.6** Régénérer `lib/types.ts` après 016 | Audit 5.2 | 🟡 non vérifiable depuis sandbox (mais `lib/types.ts:1870` lignes, n'a pas été modifié depuis le commit 3aeb485 du 20/05) |
| **P1.7** Audit RLS pré-sprint 8 | Audit 5.2 | 🟡 différé (avant sprint 8, on est encore en 7.5) |
| **P1.8** Nettoyer routes dev en prod | Audit 5.2 | 🔴 ouvert (3/5 routes sans guard) |
| **P1.9** `.gitignore` + supprimer `dev-server.log` | Audit 5.2 | 🟡 partiel — fichier `dev-server.log` (1.7 kB) présent mais non tracké git, et **pas dans `.gitignore`** |
| **P1.10** Décision WorldTides | Audit 5.2 | 🔴 ouvert (table cache `tide_cache` toujours créée mais non alimentée) |

**Bilan delta** : 15 items P0/P1 ouverts hier, 14 toujours ouverts ce soir, 1 partiel. Sprint 7.5 reste à démarrer.

### 1.4 Évolution chiffrée (vs hier — baseline `docs/AUDIT-2026-05.md`)

| Métrique | 2026-05-20 (audit fondateur) | 2026-05-21 | Δ |
|---|---|---|---|
| Commits sur `main` | dernier 8c7200a (19h35) | idem | 0 |
| LOC TS/TSX (hors tests) | non comptabilisé hier | 22 254 | n/a (premier compte) |
| `: any` / `as any` | non comptabilisé hier | **3** | n/a (baseline posé) |
| `@ts-ignore` / `@ts-expect-error` | non comptabilisé hier | **0** | n/a |
| `TODO`/`FIXME`/`XXX`/`HACK` | non comptabilisé hier | **1** (Sentry stub dans `app/(app)/carnet/error.tsx`) | n/a |
| `console.*` calls | non comptabilisé hier | **122** (dont ~80 dans `scripts/check-apis.ts`, normal pour script CLI) | n/a |
| Tests Vitest (fichiers) | 9 | 9 | 0 |
| Tests Vitest (estimés ~) | ~42 | ~42 (CLAUDE.md §2 mentionne "116 tests" — divergence à vérifier) | 0 |
| Migrations DB | 16 | 16 | 0 |
| Tables DB | 10 | 10 | 0 |
| RPC functions (count grep) | non comptabilisé | **17** | n/a |
| RLS policies | non comptabilisé | **29** | n/a |
| Indexes | non comptabilisé | **19** | n/a |
| Spots seedés | 10 | 10 | 0 |
| Guides en code (.tsx) | 3 | 3 | 0 (pas encore MDX, sprint 10) |
| Pages programmatiques `/peche/*` | 0 | 0 | 0 (sprint 10) |
| Liens cassés footer | 3 | 3 | 0 |
| `href="#"` page home | 4 | 4 | 0 |
| Mentions `[À COMPLÉTER]` pages légales | non comptabilisé | **8** | n/a (P0 nouveau) |
| Workflows GitHub Actions | 0 | 0 | 0 |
| Bundle `.next/` (local) | n/a | 492 MB | n/a (compile artefact local) |

---

## 2. État du site live (snapshot du jour)

Crawl réalisé via `mcp__workspace__web_fetch`. Limite outils : le sandbox bloque `curl` direct, donc impossible de lire les headers HTTP bruts (`Strict-Transport-Security`, `Content-Security-Policy`, etc.) — uniquement le contenu et les balises meta sont remontés. Section 4.1 sera donc en lecture incomplète.

### 2.1 Pages publiques

| URL | Statut perçu | Title | Description | Canonical / OG | Observations |
|---|---|---|---|---|---|
| `/` (home) | 200 | "Carnet de Pêche — Le réseau des pêcheurs à la canne du bord" | présente | (non explicite dans le meta — utilise probablement `metadataBase` par défaut, donc `vercel.app`) | Hero + 3 piliers + carnet + carte + communauté + témoignages + CTA bottom. Aucun changement vs 20/05. |
| `/tarifs` | 200 | "Tarifs — Carnet de Pêche" | absente du meta visible | non explicite | 3 cards, FAQ, garantie. 2 boutons "Essayer 7 jours" toujours liés au toast "sprint 4". |
| `/spots` | 200 | "Spots de pêche à la canne du bord en France — Carnet de Pêche" | présente | 🔴 `https://carnet-de-peche.vercel.app/spots` (canonical + og:url + og:image + twitter:image) | 10 spots affichés (8 Finistère + 2 Morbihan), JSON-LD ItemList probable. |
| `/spots/pointe-du-raz` | 200 | "Pêche à Pointe du Raz (29) — Bar, Lieu jaune, Maquereau · Carnet de Pêche" | présente | 🔴 `https://carnet-de-peche.vercel.app/spots/pointe-du-raz` (idem) | Solunar 7j visible, conditions complètes, mini-map. "Données de marée non disponibles" affiché proprement. |
| `/carte` | 200 | "Carte des spots de pêche — Carnet de Pêche" | présente | OG générique `Carnet de Pêche` (pas de canonical spécifique — normal app page) | Filtres affichés, "5 spots" en bas (vs 10 attendus — probablement seul `geom_public` est exposé en non-authentifié). |
| `/guides` | 200 | "Guides de pêche à la canne du bord — Carnet de Pêche" | absente du meta visible | non explicite | 3 guides listés (bar leurre, dorade surfcasting, spots Bretagne). |
| `/fil` | 🔴 **body vide** | (aucune) | aucune | aucun | Lien footer cassé — confirme P0.2 |
| `/especes` | 🔴 **body vide** | (aucune) | aucune | aucun | Lien footer cassé |
| `/techniques` | 🔴 **body vide** | (aucune) | aucune | aucun | Lien footer cassé |
| `/auth/login` | 200 | "Carnet de Pêche — Le réseau des pêcheurs à la canne du bord" | présente | (générique) | Form login, **2 `href="#"`** dans le footer micro de la page (CGU + Confidentialité) — bug nouveau pas listé dans l'audit fondateur. |
| `/auth/register` | 200 (redirige vers `/auth/login?tab=register`) | idem login | idem | idem | Friction confirmée P0 1.1.i de l'audit fondateur : inscription = login avec onglet, pas une vraie page séparée. |
| `/legal/mentions-legales` | 200 | "Mentions légales — Carnet de Pêche" | absente du meta visible | non explicite | 🔴 contient **6 mentions `[À COMPLÉTER]`** visibles publiquement |
| `/legal/confidentialite` | non crawlé en détail | présumé 200 | — | — | 🔴 contient **2 mentions `[À COMPLÉTER]`** d'après grep code |
| `/legal/cgu` | non crawlé en détail | présumé 200 | — | — | À vérifier si contient aussi des `[À COMPLÉTER]` |

### 2.2 Fichiers techniques

| URL | Statut | Contenu |
|---|---|---|
| `/sitemap.xml` | 200 | Binary (gzip) — non lisible dans cette session, à valider en navigateur. À vérifier que les URLs listées pointent vers `.com` une fois `metadataBase` corrigé. |
| `/robots.txt` | 200 | 13 lignes, propres pour les Allow/Disallow. 🔴 **`Sitemap: https://carnet-de-peche.vercel.app/sitemap.xml`** — encore une référence vercel.app à la dernière ligne. À fixer en même temps que `metadataBase`. |
| `/og/spots` | non vérifié dans ce run (image PNG) | présumé OK (sprint 4 a livré la route) |
| `/og/spot/pointe-du-raz` | non vérifié dans ce run (image PNG) | présumé OK |

### 2.3 Endpoints API publics testés

| URL | Réponse perçue | Observations |
|---|---|---|
| `/api/crons/compute-spot-scores` | body vide (probablement 401 Unauthorized sans Bearer) | Comportement attendu — sécurité fail-closed sur `CRON_SECRET`. Le body vide est suspect côté UX (devrait renvoyer un JSON `{error:'unauthorized'}` mais pas grave fonctionnellement). |
| `/api/dev-test` | body vide (sans auth → 401, c'est OK techniquement) | 🔴 La route ne devrait pas exister en prod. Aucun guard `NODE_ENV`. |
| `/api/spots/nearby` | non testé ici (POST/GET ?) | À ajouter à la checklist du prochain audit |
| `/api/health` | absent (404) | CLAUDE.md §6 le mentionnait dans la structure cible. À ajouter pour Vercel monitoring. |

### 2.4 Routes `/dev/*` et `/test`

| URL | Guard NODE_ENV ? | Comportement prod attendu |
|---|---|---|
| `/dev/solunar-preview` | ✅ oui (`if (process.env.NODE_ENV !== 'development') notFound()`) | 404 en prod (OK) |
| `/dev/scoring-preview` | ✅ oui (idem) | 404 en prod (OK) |
| `/dev/test-photo` | 🔴 non | accessible en prod (expose UI d'upload de test) |
| `/test` | 🔴 non | accessible en prod (Server Action `createCatch` exposée si user loggé — peut polluer la DB de John si on tape `/test` connecté avec son compte) |
| `/api/dev-test` | 🔴 non | accessible en prod (expose toutes les catches du user loggé en JSON, console.log de son email) |

**Recommandation** : 3 ajouts de garde `if (process.env.NODE_ENV !== 'development') notFound()` à copier-coller depuis `solunar-preview/page.tsx`. 5 minutes de boulot, fait partie du Bloc D5 du brief sprint 7.5.

---

## 3. SEO & performance technique

### 3.1 Canonical et OG

🔴 **Bug racine `metadataBase` toujours actif.** Source unique : `app/layout.tsx:19` → `new URL('https://carnet-de-peche.vercel.app')`. Conséquence en cascade observée sur le crawl du jour :

- `/spots` : `canonical = https://carnet-de-peche.vercel.app/spots`
- `/spots/pointe-du-raz` : `canonical + og:url + og:image + twitter:image` tous sur `vercel.app`
- `/robots.txt` : `Sitemap: https://carnet-de-peche.vercel.app/sitemap.xml`

**Impact SEO** : Googlebot va dédupliquer toutes les URLs sur le domaine vercel.app, et `www.carnet-de-peche.com` sera traité comme une copie secondaire. Tu perds le bénéfice SEO de ton domaine principal acheté pour la marque.

**Impact partage social** : un post Twitter/LinkedIn/WhatsApp qui partage une fiche spot affichera une image et un lien vers `carnet-de-peche.vercel.app`, ce qui fait perdre la confiance utilisateur ("c'est un site amateur sur Vercel ?") et empêche l'ancrage marque.

**Correction** : 1 ligne, 5 minutes. C'est le 1er item du Bloc A du sprint 7.5.

### 3.2 Titres, meta descriptions, h1

Échantillon sur 6 pages :

| Page | Title (chars) | Meta description (chars) | h1 unique ? | Note |
|---|---|---|---|---|
| `/` (home) | ~58 (✓ idéal 30-60) | non lisible dans le crawl | ✓ "Le carnet, la carte, et la communauté qui partage." | Bien sauf description manquante au niveau metadata route (vs root) |
| `/tarifs` | 22 (un peu court) | absente | ✓ "Une tarification simple, claire, sans piège." | À allonger pour SEO ("Tarifs Carnet de Pêche — 3 formules pour pêcher mieux" par exemple) |
| `/spots` | 56 (✓) | 122 (✓) | ✓ | OK |
| `/spots/pointe-du-raz` | 67 (un peu long) | 124 (✓) | ✓ "Pointe du Raz" | Bien |
| `/carte` | 41 (✓) | 161 (un poil long) | ✓ "Carte" (laconique) | OK |
| `/guides` | 50 (✓) | absente | ✓ "Guides pratiques pour la canne du bord" | À enrichir |
| `/auth/login` | 60 | présente (générique projet) | ✓ "Connexion à ton carnet" | OK |
| `/legal/mentions-legales` | 30 (limite basse) | absente | ✓ "Mentions légales" | OK pour légal |

**Hiérarchie h1 → h2 → h3** : pas de saut détecté sur l'échantillon home/tarifs/spots. ✓

### 3.3 JSON-LD

À auditer plus finement la prochaine fois (le crawl ne remonte que les meta, pas le `<script type="application/ld+json">`). Selon `docs/AUDIT-2026-05.md` §1.7 :
- `/spots` : JSON-LD ItemList ✓
- `/spots/[slug]` : JSON-LD Place ✓
- `/guides/*` : à vérifier (Article ?)

**À ajouter au prochain audit** : extraire le JSON-LD via une lecture du HTML brut (le crawl actuel résume le contenu, pas le markup complet).

### 3.4 Sitemap & robots.txt

- `/sitemap.xml` : 200, binary (gzip probable). Code source `app/sitemap.ts` (non lu dans ce run). À vérifier qu'il liste : home, tarifs, spots index, 10 fiches spots, guides index, 3 guides, et pages légales.
- `/robots.txt` : 13 lignes lisibles. Disallow propres (`/api/`, `/auth/callback`, `/onboarding/`, `/home`, `/profil`, `/carnet`). 🔴 dernière ligne `Sitemap:` sur vercel.app.

### 3.5 Performance proxy

Le crawl ne remonte pas le poids HTML directement. Observation indirecte :
- Home : ~80 lignes de markdown extrait → page assez dense (4 sections + 3 cartes + témoignages + CTA), probablement entre 50 et 100 kB HTML brut. À valider avec Lighthouse CI au sprint 11.
- Fiche spot : ~90 lignes de markdown — page dense aussi (météo + solunar + description + dangers + map). Idem à mesurer au sprint 11.
- Bundle local `.next/` : 492 MB total (artefacts dev). Le bundle prod sur Vercel sera très inférieur — non mesuré dans ce run.

**À ajouter au prochain audit** : `pnpm build --debug` localement (si infra le permet) pour récupérer les tailles First Load JS de chaque route.

### 3.6 OG images

Non vérifiées dans ce run (pas de moyen simple de récupérer le `Content-Type` via `web_fetch` sandboxé). À ajouter au prochain audit : `mcp__workspace__web_fetch https://www.carnet-de-peche.com/og/spot/pointe-du-raz` et vérifier que la réponse ressemble bien à du PNG (taille raisonnable, signature `‰PNG`).

---

## 4. Sécurité

### 4.1 Headers HTTP

🟡 **Non vérifiables dans ce run.** Le sandbox bloque les `curl -I` vers `www.carnet-de-peche.com` (`X-Proxy-Error: blocked-by-allowlist`). Le `mcp__workspace__web_fetch` ne remonte que les meta de body, pas les en-têtes de réponse.

**Recommandation immédiate** : John doit lancer manuellement depuis son terminal local :
```bash
curl -I https://www.carnet-de-peche.com/
```
Et vérifier la présence de `Strict-Transport-Security`, `X-Frame-Options`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, `Permissions-Policy`, `Content-Security-Policy`. Si Vercel ne les ajoute pas par défaut, c'est un Bloc D à ajouter au sprint 7.5 ou au sprint 11 (~30 min via `next.config.ts` `headers()` ou via `vercel.json`).

### 4.2 HTTPS et redirections

🟡 Non testable directement depuis ce sandbox. Vercel force HTTPS par défaut sur les domaines custom — présomption forte que c'est OK. À valider une fois en navigation manuelle : `http://www.carnet-de-peche.com` doit faire un `308 Permanent Redirect` vers `https://`.

### 4.3 Endpoints API exposés

Voir tableau 2.3 + 2.4. Résumé :
- `/api/crons/compute-spot-scores` : ✅ fail-closed Bearer secret (sprint 7)
- `/api/spots/nearby` : à auditer (rate limiting ? auth ? expose-t-il des `geom` précis si on demande un radius énorme ?)
- `/api/dev-test` : 🔴 à supprimer ou guardery (Bloc D5)
- `/dev/test-photo` : 🔴 idem
- `/test` : 🔴 idem
- `/api/health` : absent (à créer)

### 4.4 Fichiers sensibles

Non vérifiables via `web_fetch` ici. À tester manuellement (5 min) :
```bash
for url in /.env /.env.local /.git/config /package.json /pnpm-lock.yaml; do
  curl -s -o /dev/null -w "%{http_code} %s\n" "https://www.carnet-de-peche.com$url"
done
```

Comportement Vercel par défaut : ces fichiers ne sont pas servis (ils ne sont pas dans `public/`). Présomption forte de 404 sur tous, mais à valider.

### 4.5 Recherche de secrets dans le code et git log

✅ **RAS — bonne hygiène**.
- `grep -rE "(sk_live_|sk_test_|sb_secret_|sbp_|sbs_|eyJ[A-Za-z0-9_-]{30,})"` sur le code applicatif : **0 match**.
- `git log -S "SUPABASE_SERVICE_ROLE_KEY"` : seul match = commit `3aeb485` qui ajoute `lib/supabase/admin.ts` (qui lit la variable d'env, jamais sa valeur en clair).
- `git log -S "CRON_SECRET"` : seul match = même commit, ajoute la route `/api/crons/compute-spot-scores` qui lit `process.env.CRON_SECRET`.

**Pas de fuite identifiée.** À répéter à chaque audit, surtout après les sprints 9 (Stripe keys) et 11 (Sentry DSN, Resend API key, etc.).

### 4.6 RLS Supabase

Non testable depuis le sandbox sans creds. Au repo, RLS active sur toutes les tables : `migrations/002_rls.sql` (helper `has_active_subscription` + 29 policies). Nouvelle policy sur `spot_scores` ajoutée par migration 014 (lecture publique / écriture service_role).

**À auditer avant sprint 8** (P1.7) : tester manuellement avec 2 JWT distincts (user A et user B) :
- user B ne doit pas lire les catches privées de user A
- user B ne doit pas lire les spots `private` de user A
- user B ne doit pas écrire dans `spot_scores`

---

## 5. Qualité code & dépendances

### 5.1 Comptes de dette (snapshot baseline)

| Métrique | Valeur | Interprétation |
|---|---|---|
| `: any` / `: any[]` / `as any` | **3** | Excellent — 3 occurrences seulement sur 22 254 lignes |
| `@ts-ignore` / `@ts-expect-error` | **0** | Parfait |
| `TODO` / `FIXME` / `XXX` / `HACK` | **1** | Excellent — unique TODO = `app/(app)/carnet/error.tsx:15 // TODO: Sentry.captureException(error)` (prévu sprint 11) |
| `console.log/warn/error/debug/info` | **122** | Acceptable — la majorité (~80) est dans `scripts/check-apis.ts` (script CLI, normal). À reauditer la part *runtime* hors scripts. |

**Localisation précise des 3 `any`** :
- `components/conditions/TideChart.tsx:100` `(value: any) => [...]` — formatter Recharts, OK pragmatiquement
- `components/conditions/TideChart.tsx:105` `(label: any) => ...` — idem
- `lib/scoring/personal-fetcher.ts:24` `(data ?? []) as any[]` — cast Supabase, OK pragmatiquement

**Verdict** : la dette de typage est négligeable. Pas de chantier à ouvrir.

**Localisation des `console.*` hors scripts/** (échantillon) :
- `app/api/dev-test/route.ts:21-23` — 3 logs (qui partiront avec la route en suppression Bloc D5)
- `lib/scoring/spot-scores-job.ts:85` — 1 log (intentionnel, sortie cron)
- Reste : à compter ligne par ligne au prochain audit pour distinguer "logs cron/CLI utiles" vs "debug oublié".

### 5.2 Lint & typecheck

🟡 **Non lançables depuis le sandbox** (pas de `node_modules` installés ici, et `pnpm install` ne va pas tenir dans le temps imparti).

À ajouter à la checklist locale matinale de John :
```bash
pnpm lint     # cible : 0 erreur 0 warning
pnpm typecheck # cible : 0 erreur
pnpm test     # cible : 116/116 vert (selon CLAUDE.md §2)
```

Note : `next.config.ts:5` force `eslint: { ignoreDuringBuilds: true }` — donc même si `pnpm lint` casse, `pnpm build` passera. C'est un trompe-l'œil dangereux (P1.5 ouvert).

### 5.3 Tests

| Fichier | Module |
|---|---|
| `lib/catches/schema.test.ts` | zod validation catch |
| `lib/conditions/openmeteo.test.ts` | parsing API Open-Meteo |
| `lib/scoring/__tests__/catch-analysis.test.ts` | bucketing + stats |
| `lib/scoring/__tests__/insights.test.ts` | computeInsights + multipliers |
| `lib/scoring/__tests__/patterns.test.ts` | détection patterns |
| `lib/scoring/__tests__/scoring-integration.test.ts` | intégration end-to-end |
| `lib/solunar/__tests__/astronomy.test.ts` | éphémérides |
| `lib/solunar/__tests__/scoring.test.ts` | scoring solunar |
| `lib/solunar/__tests__/index.test.ts` | API publique |

**9 fichiers, ~42-116 tests selon les sources** (l'audit fondateur dit "~42", CLAUDE.md §2 dit "116 tests verts au sprint 7"). Divergence à clarifier : un `pnpm test` local te donnera le compte exact.

**Modules non testés** (acceptable pour MVP) :
- `lib/catches/queries.ts`, `lib/catches/actions.ts` (Server Actions)
- `lib/conditions/spot-forecast.ts` (très utilisé sur fiche spot)
- `lib/supabase/*` (clients)
- Tous les composants UI

**Pas de test E2E** (Playwright sprint 11), pas de **test RLS** (P1.7), pas de test du **cron** lui-même.

### 5.4 Dépendances

`package.json` extrait (déps prod) :

- Next 15.5.18 ✓
- React 19 ✓
- Tailwind 4.3.0 ✓
- @supabase/ssr 0.10.3 + supabase-js 2.105.4 ✓
- MapLibre 5.24.0 ✓
- Recharts 3.8.1 ✓
- @base-ui/react 1.4.1 ✓ (Drawer)
- shadcn 4.7.0 — ⚠️ **toujours en deps prod** alors que c'est un CLI (signalé hier). Devrait être devDep.
- suncalc 1.9.0 + @types/suncalc ✓
- ngeohash 0.6.3 + @types/ngeohash 0.6.8 — ⚠️ **toujours suspect** : pas vu d'import dans le code applicatif (signalé hier, non investigué encore).
- exifr 7.1.3 ✓ (EXIF GPS)
- browser-image-compression 2.0.2 ✓
- date-fns 4.1.0 ✓
- lucide-react 1.14.0 — version récente, OK
- zod 4.4.3 ✓
- ESLint 9.39.4 + eslint-config-next 16.2.6 + @eslint/eslintrc 3.3.5 (flat config)

`pnpm outdated` et `pnpm audit` non lançables depuis ce sandbox. À automatiser dans un futur workflow CI.

### 5.5 Taille du repo

| Métrique | Valeur |
|---|---|
| Total LOC TS/TSX hors tests | **22 254** |
| Plus gros fichier | `lib/types.ts` (1870 lignes — généré Supabase) |
| Plus gros fichier app | `components/catches/CatchForm.tsx` (964 lignes — à découper sprint polish ?) |
| 2e plus gros app | `app/(app)/onboarding/[step]/onboarding-step.tsx` (692 lignes) |
| Bundle `.next/` local | 492 MB |

**Top 10 fichiers** (lignes) :
```
1870  lib/types.ts                                          (auto-gen)
 964  components/catches/CatchForm.tsx                      (à découper ?)
 692  app/(app)/onboarding/[step]/onboarding-step.tsx       (à découper ?)
 628  components/map/MapShell.tsx
 557  app/(marketing)/spots/[slug]/page.tsx                 (à découper ?)
 543  app/auth/login/page.tsx
 507  app/(marketing)/page.tsx                              (légitime, landing)
 479  components/map/MapView.tsx
 435  lib/conditions/spot-forecast.ts
 412  components/map/SpotPopup.tsx
```

`CatchForm.tsx` et `onboarding-step.tsx` méritent un refactor en sprint 11 (polish) : >700 lignes par composant React, c'est limite pour la lisibilité.

### 5.6 Configuration critique

| Fichier | Setting | État | Note |
|---|---|---|---|
| `app/layout.tsx:19` | `metadataBase` | 🔴 `vercel.app` | P0.1 |
| `lib/env.ts` | Zod schema | 🔴 incomplet | Manque `CRON_SECRET` + `SUPABASE_SERVICE_ROLE_KEY` (P1.3) |
| `next.config.ts:5` | `eslint.ignoreDuringBuilds` | 🔴 `true` | P1.5 |
| `next.config.ts:13` | Unsplash dans `remotePatterns` | ⚠️ probablement non utilisé | À retirer (backlog) |
| `tsconfig.json` | `strict` | ✅ true | OK |
| `.github/workflows/` | CI | 🔴 absent | P1.4 |
| `vercel.json` | crons | ✅ `0 5 * * *` (daily, plan Hobby) | OK depuis fix 33f2037 du 20/05 |
| `.gitignore` | `dev-server.log` | 🔴 absent | P1.9 partiel — fichier non tracké mais peut être ré-ajouté accidentellement |
| `pnpm-workspace.yaml` | monorepo | ⚠️ stub (`allowBuilds` seulement) | Pas de migration Turborepo amorcée — OK on est encore en MVP web only |

---

## 6. DB Supabase & contenu

### 6.1 Migrations

**16 migrations** présentes dans `supabase/migrations/`, dernière timestamp = 20 mai 13:56. Identique à l'audit fondateur.

```
001_init.sql                                  (11 kB, 11 mai)
002_rls.sql                                   (7.6 kB)
003_indexes_views.sql                         (4.6 kB)
004_functions_triggers.sql                    (7.0 kB)
005_profile_extension.sql                     (1.1 kB, 12 mai)
006_catches_storage_extension.sql             (7.6 kB) — inclut tide_cache stub
007_catch_stats_rpc.sql                       (1.0 kB)
008_catch_extended_stats.sql                  (1.4 kB)
009_map_helpers.sql                           (2.5 kB, 19 mai)
010_fix_get_spots_for_map_precise.sql         (2.1 kB)
011_get_spot_by_slug.sql                      (2.1 kB)
012_get_spot_by_id.sql                        (1.6 kB)
013_catches_scoring_columns.sql               (3.3 kB, 20 mai 12:52)
014_spot_scores.sql                           (3.0 kB)
015_catches_scoring_columns_reconcile.sql     (3.3 kB) — réconciliation drift
016_get_spots_for_scoring.sql                 (1.2 kB, 20 mai 13:56)
```

Aucune nouvelle migration depuis hier — cohérent avec 0 commit.

### 6.2 Structure inférée

| Élément | Compte (via grep) |
|---|---|
| Tables | **10** |
| RPC functions | **17** |
| RLS policies | **29** |
| Indexes (B-tree + GiST + GIN + unique) | **19** |

**Verdict** : structure de schéma riche et défensive pour cette taille de projet. Pas de chantier urgent.

### 6.3 Drift schema

🟡 Non vérifiable sans creds Supabase ici. La migration 015 documente déjà un drift local/remote (cf audit fondateur §2.3). À tester manuellement :
```bash
supabase db diff --linked
```
Cible : `No schema difference found`. Sinon, documenter le drift et créer une migration 017 de réconciliation.

### 6.4 Contenu

| Type | Compte | Note |
|---|---|---|
| Spots seedés (`supabase/seed.sql`) | **10** | 8 Finistère + 2 Morbihan. À enrichir à ≥100 avant lancement public (sprint 21). |
| Guides en code (TSX) | **3** | bar leurre, dorade surfcasting, spots Bretagne. À migrer en MDX au sprint 10. |
| Pages programmatiques `/peche/[espece]/[technique]` | **0** | Sprint 10. |
| Pages SEO-visibles `/fil`, `/especes`, `/techniques` | **0** | 🔴 P0.2 (sprint 7.5 ou 8). |
| Drafts guides dans `docs/guides/` | non recensé ici | À auditer demain. |

---

## 7. Accessibilité & UX

### 7.1 Échantillon a11y (home, tarifs, fiche spot)

- ✅ `<html lang="fr">` présent (Next.js par défaut + config). Cookie `meta-og:locale: fr_FR` constaté.
- ✅ Un seul `<h1>` par page sur l'échantillon (home, tarifs, spots).
- ✅ `<main>` présent (vérifié sur `/tarifs`).
- 🟡 `<button>` vs `<a>` : non échantillonné en profondeur. Les CTAs principaux sont des `<a href="...">`, ce qui est OK sémantiquement.
- 🟡 `alt` sur les `<img>` : non échantillonné. À ajouter au prochain audit (lecture de `Image` Next + alt).
- 🟡 Form labels : `<label>` présent sur le form login (l'extraction markdown les a remontés via "Ton email"/"Mot de passe" comme texte de label). À valider en HTML brut au prochain audit.
- ✅ Viewport meta présent sur toutes les pages crawlées.

**Bilan a11y** : pas de red flag visible, mais audit superficiel. À refaire avec axe-core au sprint 11.

### 7.2 Tone & copy

- ✅ Tutoiement systématique sur les pages publiques crawlées (home, tarifs, spots, guides, fiche spot). Aucun "vous" suspect trouvé hors pages légales.
- ✅ Pages légales : tutoiement utilisé même dans CGU/Mentions ("tu peux nous contacter", "Tu choisis ce que tu partages"). Bien.
- 🔴 **Cohérence pricing** : "14j" sur la home (`page.tsx:257`), "7 jours" sur `/tarifs` (titre + bouton + FAQ + bandeau bottom de la home `page.tsx:479`). **Toujours incohérent.** L'utilisateur qui scroll la home voit "14j", clique "Voir les tarifs", et tombe sur "7 jours". P0.3.
- 🔴 **Sprint X mentionné dans l'UI publique** : "App iOS / Android (sprint 13+)" sur la card Découverte (`pricing-cards.tsx:21`). + Toast "Les paiements Stripe arrivent lors du sprint 4" (`pricing-cards.tsx:69`). À nettoyer.
- 🔴 **CTAs cassés (`href="#"`)** : 4 sur la home (lignes 244, 342, 384, 494) + 2 sur `/auth/login` (footer CGU/Confidentialité). Total **6 liens morts** sur les pages les plus visitées.
- 🔴 **Promesses non livrées toujours en home** : "Import / export GPX, JSON, Fishbrain, FishFriender", "Modération humaine — Pêcheurs ambassadeurs régionaux + IA", "Mode hors ligne", "217 spots actifs aujourd'hui", "Toute prise publique est positionnée dans un rayon de 2 km" (alors que la migration 004 dit 1 km). Cf audit fondateur §1.1.
- 🔴 **Témoignages fictifs** : Yann L. (Finistère), Julien R. (Morbihan), François B. (Charente-Maritime) toujours affichés sur la home avec 5 étoiles. Risque L121-4 si un compétiteur ou journaliste creuse.

---

## 8. Veille concurrence

📅 **Programmée le lundi prochain (25 mai 2026).** Le rituel hebdo couvre spot-de-peche.com et fishfriender.com — pas d'observation aujourd'hui.

Rappel du positionnement actuel (cf CLAUDE.md §1) :
- **vs spot-de-peche.com** : carte + données environnementales sont des table stakes (sprint 4 livre la parité), différenciateurs = scoring personnalisé (sprint 7 ✓) + carnet personnel + fil régional (sprint 8) + mobile (sprint 13+)
- **vs FishFriender** : hyper-spécialisation canne du bord + FR + carte basique gratuite + fil par département

Pas de changement attendu cette semaine côté concurrence (pas de signal vu lors de l'audit fondateur).

---

## 9. Risques détectés aujourd'hui

| # | Risque | Probabilité | Impact | Statut | Évolution vs hier |
|---|---|---|---|---|---|
| R1 | Désindexation Google du domaine `.com` (canonical vers `.vercel.app`) | Haute | Très fort | 🔴 actif | persistant (24 jours) |
| R2 | 3 liens footer cassés perçus comme "site broken" | Haute | Moyen | 🔴 actif | persistant |
| R3 | Pages légales avec `[À COMPLÉTER]` publiques | **Haute** | **Fort** (risque légal pré-Stripe) | 🔴 actif | **NOUVEAU — pas listé hier** |
| R4 | Témoignages fictifs (article L121-4 Code conso) | Faible-moyenne | Fort | ⚠️ actif | persistant |
| R5 | Promesses non livrées sur home (import/export, ambassadeurs, hors ligne) | Haute | Moyen | ⚠️ actif | persistant |
| R6 | Pricing 7j vs 14j incohérent | Haute | Moyen | ⚠️ actif | persistant |
| R7 | 6 CTAs morts (`href="#"`) sur pages les plus visitées (home + login) | Haute | Faible-moyen | ⚠️ actif | persistant (et **+2 nouveaux non listés hier** sur /auth/login pour CGU/Confi) |
| R8 | Routes dev exposées en prod (`/test`, `/dev/test-photo`, `/api/dev-test`) | Moyenne | Faible-moyen | ⚠️ actif | persistant (P1.8) |
| R9 | Drift schema local/remote (migration 015 documente déjà un drift) | Moyenne | Fort | ⚠️ actif | persistant (P1.2) |
| R10 | `env.ts` ne valide pas `CRON_SECRET`/`SERVICE_ROLE_KEY` → silent fail futur | Faible | Fort | ⚠️ actif | persistant (P1.3) |
| R11 | Pas de monitoring (Sentry) — cécité totale | Certaine | Fort | ⚠️ actif | prévu sprint 11 |
| R12 | Pas d'analytics (Plausible / PostHog) | Certaine | Moyen | ⚠️ actif | prévu sprint 11 |
| R13 | Pas de CI (lint + typecheck + tests) | Haute | Moyen | ⚠️ actif | persistant (P1.4) |
| R14 | `ignoreDuringBuilds: true` masque les régressions lint | Moyenne | Faible-moyen | ⚠️ actif | persistant (P1.5) |
| R15 | Headers HTTP de sécurité non vérifiables ici (probablement défaut Vercel) | Inconnue | Inconnu | 🟡 à investiguer | non testé hier |
| R16 | `/test` Server Action `createCatch` accessible en prod sur compte loggé → peut polluer DB de John | Faible | Faible | ⚠️ actif | non explicitement noté hier |

**Risques résolus depuis hier** : aucun (0 commit applicatif).

---

## 10. Top 5 actions recommandées pour demain (vendredi 22 mai)

Ordre d'attaque proposé pour optimiser le ratio impact/coût et débloquer le sprint 7.5.

### 1. **Bloc A1 — Fix `metadataBase` (5 minutes, impact SEO immédiat)**
- Fichier : `app/layout.tsx:19`
- `new URL('https://carnet-de-peche.vercel.app')` → `new URL('https://www.carnet-de-peche.com')`
- Commit + redeploy Vercel
- Validation : `curl -s https://www.carnet-de-peche.com/spots/pointe-du-raz | grep canonical` doit renvoyer `https://www.carnet-de-peche.com/...`

### 2. **Bloc A2 — Footer stubs `/fil`, `/especes`, `/techniques` (1h)**
- Créer 3 pages `app/(marketing)/fil/page.tsx`, `especes/page.tsx`, `techniques/page.tsx` avec un message "Bientôt disponible — inscris-toi pour être notifié" + meta SEO + un CTA register
- Ces stubs commencent aussi à construire de la valeur SEO pour les requêtes "fil de pêche [dept]" / "espèces pêche bord mer" / "techniques pêche canne"

### 3. **🚨 Bloc 0 NOUVEAU — Compléter mentions légales (30 min)**
- Remplir les **8 `[À COMPLÉTER]`** dans `app/(marketing)/legal/mentions-legales/page.tsx` (6) et `app/(marketing)/legal/confidentialite/page.tsx` (2)
- Décider : forme juridique (EI / SASU / SAS), SIRET, adresse, directeur de publication
- **Bloquant** avant tout paiement Stripe (sprint 9). Risque légal sérieux si laissé en l'état.
- Ne pas oublier de vérifier aussi `/legal/cgu`

### 4. **Bloc A6 — Aligner copy home avec la réalité (1h)**
- Retirer ou marquer "bientôt" : "Import/export GPX/Fishbrain", "Modération humaine ambassadeurs", "Mode hors ligne", "217 spots actifs"
- Floutage : "2 km" → "1 km" (`app/(marketing)/page.tsx:408`)
- Stat hero "14j" → "7j" (ou inverse — voir Bloc A3 pour décider) `page.tsx:257`
- Témoignages : retirer (la beta n'a pas eu lieu)

### 5. **Bloc A4 + A5 — CTAs `#` et toast "sprint 4" (45 min)**
- 4 `href="#"` home → soit `/carte` (3 fois), soit `/auth/register` (1 fois pour "Voir un carnet exemple")
- 2 `href="#"` footer login → vraies routes `/legal/cgu` et `/legal/confidentialite`
- `pricing-cards.tsx:69` toast → soit retirer le toast et activer la redirection `<Link href="/auth/register?plan=local">`, soit garder un message neutre sans mention "sprint X"

**Total estimé** : ~3h30 de coding + 30 min de relecture/QA + 15 min de redeploy. Tu peux fermer 5 des 7 P0/🚨 listés dans une seule journée.

**Bloc D5** (cleanup routes dev) à ajouter en bonus si la journée le permet (~15 min).

---

## 11. Bilan vs roadmap

### 11.1 Sprint en cours

**Sprint 7.5 — Polish marketing / SEO / infra**
- Démarrage prévu : 20 mai (jour de l'audit fondateur)
- Démarrage effectif : pas encore amorcé (au 21/05 02h47 Paris)
- Durée prévue : 3-5 jours
- Brief : `docs/sprint-7.5/brief-sprint-7.5.md` (574 lignes, 4 blocs A/B/C/D)
- Critères de sortie : checklist 16 items en bas du brief

**Avancement Bloc par bloc** :

| Bloc | Sujet | Tâches | Faites | À faire |
|---|---|---|---|---|
| A | Marketing & SEO | 7 (metadataBase, footer, pricing, CTAs, copy home, témoignages) | 0/7 | toutes |
| B | Dette sprint 7 | 1 (badge "⚡ Perso" inerte) | 0/1 | 1 |
| C | Dette lint | 1 (365 erreurs react/no-unescaped-entities + retrait ignoreDuringBuilds) | 0/1 | 1 |
| D | Infra | 8 (env.ts, migrations diff, types regen, CI, cleanup dev routes, gitignore log, Vercel env vars, cron smoke test) | 1/8 (D7 partiel — vars Vercel ajoutées au commit 97132cb du 20/05) | 7 |

**Avancement global sprint 7.5** : ~5 % (1 item partiellement validé sur ~17).

### 11.2 Roadmap globale

Pas de retard structurel. Le sprint 7.5 est un sprint inséré entre 7 et 8, dimensionné à 1 semaine. À condition que tu démarres demain et que tu enchaînes 4-5 jours, tu pourrais entrer en sprint 8 (fil communautaire) **dès le 27 ou 28 mai**, ce qui est cohérent avec le découpage du `docs/ROADMAP.md`.

### 11.3 Prochains gates

| Gate | Date cible | État |
|---|---|---|
| Fin sprint 7.5 (checklist 16 items verts) | ~27 mai 2026 | À démarrer |
| Sprint 8 (fil régional + tables `feed_*`) | ~28 mai → ~10 juin | Bloqué par 7.5 et par audit RLS pré-flight |
| Sprint 9 (Stripe) | ~10 juin → ~24 juin | **Bloqué par mentions légales** (🚨 nouveau risque détecté ce soir) |
| Beta privée 50 testeurs (sprint 11) | ~22 juillet → ~5 août | OK |
| Lancement public iOS+Android (sprint 21) | ~janvier 2027 | OK (saisonnalité pêche favorable) |

### 11.4 KPIs à mettre en place avant beta

Hors scope de cet audit, mais à programmer dans le sprint 11 (déjà prévu dans ROADMAP.md) :
- North star : prises loguées par utilisateur actif / semaine
- Activation : % users qui loguent ≥1 prise dans les 7 jours après inscription
- Rétention W1 / W4
- Conversion gratuit → Local (objectif ≥5 % à 30 jours)
- Conversion Local → Itinérant (objectif ≥10 %)

---

## Annexes

### A. Commandes utiles pour ce sprint 7.5

```bash
# Quick wins matinaux
cd ~/Carnet-de-peche

# 1. Fix metadataBase (5 min)
sed -i "s|carnet-de-peche.vercel.app|www.carnet-de-peche.com|g" app/layout.tsx

# 2. Lint local (vérification baseline)
pnpm lint 2>&1 | tee /tmp/lint-$(date +%F).log

# 3. Tests (cible 116/116)
pnpm test

# 4. Diff DB
supabase db diff --linked

# 5. Stats post-cron
psql $(supabase status -o json | jq -r '.DB_URL') -c "select count(*) from spot_scores;"
```

### B. Sources crawlées dans ce run

- https://www.carnet-de-peche.com/
- https://www.carnet-de-peche.com/tarifs
- https://www.carnet-de-peche.com/spots
- https://www.carnet-de-peche.com/spots/pointe-du-raz
- https://www.carnet-de-peche.com/carte
- https://www.carnet-de-peche.com/guides
- https://www.carnet-de-peche.com/fil (body vide)
- https://www.carnet-de-peche.com/especes (body vide)
- https://www.carnet-de-peche.com/techniques (body vide)
- https://www.carnet-de-peche.com/auth/login
- https://www.carnet-de-peche.com/auth/register
- https://www.carnet-de-peche.com/legal/mentions-legales
- https://www.carnet-de-peche.com/sitemap.xml (binary)
- https://www.carnet-de-peche.com/robots.txt
- https://www.carnet-de-peche.com/api/crons/compute-spot-scores (vide, attendu 401)
- https://www.carnet-de-peche.com/api/dev-test (vide, attendu 401)

### C. Limitations de ce run

- Le sandbox bloque `curl` direct vers `www.carnet-de-peche.com` → headers HTTP non visibles (HSTS, CSP, X-Frame-Options, etc.)
- `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm outdated`, `pnpm audit` non lançables (pas de `node_modules` dans le sandbox)
- `supabase db diff` non lançable (pas de creds)
- Vérification visuelle des OG images, JSON-LD, et a11y profonde reportée au prochain audit (besoin d'outils supplémentaires)

### D. Process check

Premier audit du rituel quotidien. Le dossier `docs/audits/` a été créé et un `README.md` y a été ajouté pour expliquer le format.

À la fin de chaque audit, le fichier `AUDIT-YYYY-MM-DD.md` est présenté à John pour qu'il l'ouvre directement le matin.

Pas de push, pas de commit — l'audit n'écrit que dans `docs/audits/`.

### E. Détail migration par migration (rappel pour contexte)

| # | Fichier | Taille | Ajouts clés | Risque résiduel |
|---|---|---|---|---|
| 001 | `001_init.sql` | 11 kB | Extensions postgis/pgcrypto/citext/pg_trgm, types enum, 10 tables | OK |
| 002 | `002_rls.sql` | 7.6 kB | RLS activée partout + helper `has_active_subscription(uid)` | OK |
| 003 | `003_indexes_views.sql` | 4.6 kB | Index PostGIS GiST, B-tree, GIN + vues `catches_for_viewer`, `spots_for_viewer` | OK |
| 004 | `004_functions_triggers.sql` | 7.0 kB | `handle_new_user`, `touch_updated_at`, floutage GPS, `nearby_spots()` | OK |
| 005 | `005_profile_extension.sql` | 1.1 kB | RPC `delete_my_account()` (RGPD) | OK |
| 006 | `006_catches_storage_extension.sql` | 7.6 kB | ALTER catches + bucket photos + table `tide_cache` (WorldTides) | ⚠️ `tide_cache` créé jamais alimenté (P1.10) |
| 007 | `007_catch_stats_rpc.sql` | 1.0 kB | RPC `get_my_catch_stats` | OK |
| 008 | `008_catch_extended_stats.sql` | 1.4 kB | RPC `get_my_catches_breakdown` | OK |
| 009 | `009_map_helpers.sql` | 2.5 kB | RPC `get_spots_for_map` | OK |
| 010 | `010_fix_get_spots_for_map_precise.sql` | 2.1 kB | Bug fix `geom` vs `geom_public` | OK |
| 011 | `011_get_spot_by_slug.sql` | 2.1 kB | RPC fiche spot par slug | OK |
| 012 | `012_get_spot_by_id.sql` | 1.6 kB | RPC fiche spot par UUID | OK |
| 013 | `013_catches_scoring_columns.sql` | 3.3 kB | Colonnes `wind_speed_kmh`, `wind_direction_deg`, `tide_state`, `location_label` | ⚠️ migration en partie déjà appliquée à la main au remote = origine du drift |
| 014 | `014_spot_scores.sql` | 3.0 kB | Table `spot_scores` + RLS | OK |
| 015 | `015_catches_scoring_columns_reconcile.sql` | 3.3 kB | Réconciliation 013 + vue divergée `catches_for_viewer` | ⚠️ documente le drift (P1.2) |
| 016 | `016_get_spots_for_scoring.sql` | 1.2 kB | RPC `security definer` révoquée du `public` (service_role only) | OK |

**Total** : 10 tables, 17 RPC functions, 29 RLS policies, 19 indexes. Densité saine pour cette taille de projet.

### F. Patches recommandés pour demain (copy-paste prêts)

#### Patch 1 — `metadataBase` (Bloc A1)

```diff
--- a/app/layout.tsx
+++ b/app/layout.tsx
@@ -17,7 +17,7 @@
 const APP_DESC = "Le carnet de pêche numérique et le réseau communautaire des pêcheurs à la canne du bord. Logue tes prises, suis les conditions, retrouve les bons spots — entre passionnés.";

 export const metadata: Metadata = {
-  metadataBase: new URL('https://carnet-de-peche.vercel.app'),
+  metadataBase: new URL('https://www.carnet-de-peche.com'),
   title: { default: `${APP_NAME} — Le réseau des pêcheurs à la canne du bord`, template: `%s — ${APP_NAME}` },
   description: APP_DESC,
```

#### Patch 2 — Footer stubs (Bloc A2)

Créer 3 nouveaux fichiers avec ce squelette (adapté à chaque page) :

```tsx
// app/(marketing)/fil/page.tsx
import type { Metadata } from "next";
import Link from "next/link";

export const metadata: Metadata = {
  title: "Fil régional — Bientôt",
  description: "Le fil régional des pêcheurs à la canne du bord par département. Conditions du jour, prises récentes, alertes locales. Bientôt disponible.",
};

export default function FilPage() {
  return (
    <main className="max-w-2xl mx-auto px-6 py-16 text-center">
      <span className="text-[12px] font-semibold uppercase tracking-widest text-teal-600">Bientôt</span>
      <h1 className="font-display text-navy-900 text-4xl mt-3 mb-4">Le fil régional arrive</h1>
      <p className="text-ink-600 text-lg mb-8">
        Conditions du jour, prises récentes, alertes locales — par département. Disponible cet été.
      </p>
      <Link
        href="/auth/register"
        className="inline-block rounded-xl bg-teal-500 px-6 py-3 font-semibold text-white hover:bg-teal-600"
      >
        Crée ton carnet — je te notifie au lancement
      </Link>
    </main>
  );
}
```

Dupliquer pour `especes/page.tsx` et `techniques/page.tsx` en adaptant le wording (et idéalement enrichir avec preview du contenu à venir pour la valeur SEO).

#### Patch 3 — `env.ts` (Bloc D1, P1.3)

```diff
--- a/lib/env.ts
+++ b/lib/env.ts
@@ -1,16 +1,21 @@
 import { z } from "zod";

+const isProduction = process.env.VERCEL_ENV === 'production';
+
 const envSchema = z.object({
   NEXT_PUBLIC_SUPABASE_URL: z.string().url(),
   NEXT_PUBLIC_SUPABASE_ANON_KEY: z.string().min(1),
   SUPABASE_PROJECT_REF: z.string().min(1).optional(),
+  // Requis runtime serveur (cron + admin client). Optionnels en dev local.
+  CRON_SECRET: isProduction ? z.string().min(20) : z.string().min(20).optional(),
+  SUPABASE_SERVICE_ROLE_KEY: isProduction ? z.string().min(20) : z.string().min(20).optional(),
 });

 const _env = envSchema.safeParse({
   NEXT_PUBLIC_SUPABASE_URL: process.env.NEXT_PUBLIC_SUPABASE_URL,
   NEXT_PUBLIC_SUPABASE_ANON_KEY: process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
   SUPABASE_PROJECT_REF: process.env.SUPABASE_PROJECT_REF,
+  CRON_SECRET: process.env.CRON_SECRET,
+  SUPABASE_SERVICE_ROLE_KEY: process.env.SUPABASE_SERVICE_ROLE_KEY,
 });
```

#### Patch 4 — Guard dev routes (Bloc D5)

À ajouter en tête de `app/test/page.tsx`, `app/dev/test-photo/page.tsx`, `app/api/dev-test/route.ts` :

```tsx
import { notFound } from 'next/navigation'

export const dynamic = 'force-dynamic'

// Le guard en tête de module garantit qu'aucun pré-render ne fuit en prod
if (process.env.NODE_ENV !== 'development') {
  notFound()
}
```

(Pour `route.ts`, retourner une `NextResponse` 404 au lieu de `notFound()`.)

#### Patch 5 — `.gitignore` (Bloc D6)

```diff
--- a/.gitignore
+++ b/.gitignore
@@ -38,3 +38,4 @@
 .yarn/install-state.gz
 .pnp.*
+dev-server.log
```

#### Patch 6 — CI minimal GitHub Actions (Bloc D4, P1.4)

```yaml
# .github/workflows/check.yml
name: check
on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
```

### G. Checklist de sortie sprint 7.5 — état au 21/05

Reprise des 16 items de `docs/sprint-7.5/brief-sprint-7.5.md` (bas du document) :

| # | Item | État au 21/05 |
|---|---|---|
| 1 | `pnpm lint` = 0 erreur | 🔴 non vérifié (sandbox limit) — l'audit fondateur §2.7 mentionnait ~365 erreurs `react/no-unescaped-entities` |
| 2 | `pnpm typecheck` = 0 erreur | 🟡 supposé OK (vu que les tests tournent dans CLAUDE.md §2) |
| 3 | `pnpm test` = 116/116 vert | 🟡 supposé OK — CLAUDE.md §2 mentionne 116 vert au sprint 7 mais grep ne compte que ~42 tests fichiers… écart à clarifier |
| 4 | `pnpm build` passe sans `ignoreDuringBuilds` | 🔴 non — `next.config.ts:5` toujours actif |
| 5 | CI GitHub Actions vert sur `main` | 🔴 non — workflow absent |
| 6 | `/spots/pointe-du-raz` retourne `canonical = .com/...` | 🔴 non — canonical toujours `vercel.app` |
| 7 | Footer : 3 liens `/fil`, `/especes`, `/techniques` OK | 🔴 non |
| 8 | Pricing live cohérent (7j ou 14j partout) | 🔴 non |
| 9 | Page `/tarifs` : "Essayer X jours" redirige `/auth/register` | 🔴 non — toast "sprint 4" actif |
| 10 | Home : aucun `href="#"`, aucune mention "import/export", "ambassadeurs", "217 spots actifs", "2 km" | 🔴 non — toutes présentes |
| 11 | Témoignages fictifs retirés ou clairement marqués | 🔴 non |
| 12 | Fiches spots : plus de badge "⚡ Perso" inerte | 🔴 non — `SpotBestMomentsSection.tsx:47-49` toujours actif |
| 13 | `/profil` : `PersonalScoreSection` fonctionne (mode descriptif) | 🟢 livré au commit `044fc80` du 20/05 |
| 14 | Vercel : `CRON_SECRET` + `SUPABASE_SERVICE_ROLE_KEY` configurés en prod | 🟢 livré au commit `97132cb` du 20/05 |
| 15 | Cron a tourné au moins 1× : `spot_scores` ≥ 10 lignes | 🟡 cron passé à daily (`0 5 * * *`) au commit `33f2037`. Premier run prévu 21/05 à 05h00 UTC. **À vérifier en milieu de matinée demain**. |
| 16 | Carte live : markers colorisés (pas tous gris) | 🟡 dépendant du #15 |
| 17 | `supabase db diff --linked` = "no schema difference" | 🔴 non vérifié (P1.2 ouvert) |
| 18 | CLAUDE.md §2 mis à jour : sprint 7 ✅ + sprint 7.5 ✅ | 🟡 partiel (CLAUDE.md §2 mentionne déjà 7.5 comme "MAINTENANT" mais pas encore comme fait) |

**Bilan** : 2 items verts (13, 14), 4 jaunes (3, 15, 16, 18), 12 rouges. Sprint 7.5 ~11 % complété.

### H. Détail tone/copy — extraits littéraux à corriger

Voici les chaînes exactes à modifier dans `app/(marketing)/page.tsx` pour le Bloc A6 :

| Ligne | Chaîne actuelle | Recommandation |
|---|---|---|
| 257 | `{ num: "14j", label: "Essai gratuit garanti" }` | `{ num: "7j", label: "Essai gratuit garanti" }` (si décision = 7 jours) |
| 338 | `"Import / export GPX, JSON, Fishbrain, FishFriender. Tes données sont à toi."` | Retirer la card OU la marquer "Bientôt — Import/export GPX, JSON" sans citer les concurrents |
| 374 | `<div className="font-display ...">217</div>` (Spots actifs aujourd'hui) | Remplacer par chiffre réel (probablement 0 ou 1 sans beta) OU reformuler "Plus de 200 spots ciblés au lancement" |
| 408 | `"Toute prise publique est positionnée dans un rayon de 2 km. Anti spot-burning."` | `"Toute prise publique est positionnée dans un rayon de 1 km. Anti spot-burning."` |
| 409 | `"Pêcheurs ambassadeurs régionaux + IA pour signaler les abus."` | `"Modération communautaire (signalement utilisateurs) — IA en renfort à terme."` |
| 430-441 | Témoignages Yann L., Julien R., François B. | Retirer la section OU remplacer par "Beta privée ouverte cet été — laisse ton email" |
| 244, 342, 384, 494 | `href="#"` | `href="/carte"` (×3) ou `href="/auth/register"` (×1 pour "Voir un carnet exemple") |

Pour `app/(marketing)/tarifs/pricing-cards.tsx` :

| Ligne | Chaîne actuelle | Recommandation |
|---|---|---|
| 21 | `"App iOS / Android (sprint 13+)"` | `"App iOS / Android (bientôt)"` |
| 67-71 | `function handleTrialClick() { toast.info(..., 'Les paiements Stripe arrivent lors du sprint 4.') }` | Soit retirer la fonction et faire `<Link href="/auth/register?plan=local">`, soit toast neutre `"Active ton essai lors de l'inscription"` |
| 36 | `"Mode hors ligne (carte + marées 7 jours)"` | Marquer "(bientôt, sprint mobile)" OU retirer |
| 37 | `"Notifications push créneaux optimaux"` | Idem |
| 38 | `"Fil régional en écriture + interactions sociales"` | Marquer "(bientôt, été 2026)" |

Pour `app/auth/login/page.tsx` (footer micro) :

```diff
- © 2026 Carnet de Pêche · [CGU](#) · [Confidentialité](#)
+ © 2026 Carnet de Pêche · [CGU](/legal/cgu) · [Confidentialité](/legal/confidentialite)
```

### I. Notes pour le prochain audit (vendredi 22 mai, 20h)

Améliorations à apporter à la procédure d'audit pour gagner en profondeur :

1. **JSON-LD complet** : récupérer le HTML brut (sans summary) pour valider la structure des Schema.org sur `/spots`, `/spots/[slug]`, `/guides/*`.
2. **OG images** : valider que `/og/spot/pointe-du-raz` retourne bien un PNG (`Content-Type: image/png`, taille < 500 kB).
3. **Pages légales restantes** : crawler explicitement `/legal/confidentialite` et `/legal/cgu` pour vérifier qu'aucun autre `[À COMPLÉTER]` ne traîne, et que les pages sont à jour avec la stack actuelle (Resend, etc.).
4. **Headers HTTP** : demander à John de lancer `curl -I` depuis son terminal local et coller la sortie dans une issue / un fichier `docs/audits/_headers-snapshot.md` (mensuel).
5. **Lighthouse local** : si John lance un `npx @lhci/cli` chaque vendredi, on peut tracker l'évolution Perf/A11y/SEO/Best Practices.
6. **Vérifier le run du cron du 21/05 05h00 UTC** : `select count(*), max(computed_at) from spot_scores;` → doit retourner ≥ 10 et un timestamp ≤ 3h. Si 0 ou stale, le `CRON_SECRET` côté Vercel ne match pas.
7. **Diff sitemap.xml** : extraire le contenu (en clair) du sitemap pour valider qu'il liste bien les 10 fiches spots + 3 guides + pages marketing, et que toutes les URLs sont sur `.com`.
8. **Diff `pnpm outdated`** : à automatiser dans un job CI mensuel pour suivre la dérive de versions.

### J. Suggestion d'amélioration du brief sprint 7.5

Le brief actuel (`docs/sprint-7.5/brief-sprint-7.5.md`) ne mentionne pas explicitement le risque mentions légales. Suggestion : **ajouter un Bloc 0 (juridique) au tout début**, en pré-requis avant tout Bloc A :

> **Bloc 0 — Juridique (30 min, bloquant pour sprint 9)**
>
> Remplir les 8 `[À COMPLÉTER]` dans :
> - `app/(marketing)/legal/mentions-legales/page.tsx` (6 champs : dénomination, forme juridique, SIRET, adresse, directeur, TVA)
> - `app/(marketing)/legal/confidentialite/page.tsx` (2 champs : dénomination, adresse)
>
> Décisions à acter en amont :
> - Forme juridique (EI / SASU / SAS) — recommandation : EI si CA prévu < 50k€/an la 1re année, SASU si projection levée de fonds ou associé futur
> - Adresse du siège : domicile perso ou domiciliation pro
> - Directeur de publication : par défaut, le fondateur (John)
>
> Sans cette correction, **toute prise de paiement Stripe (sprint 9) expose à L. 121-1 et R. 111-1 du Code de la consommation** (information obligatoire des consommateurs).

À John de décider de l'intégrer ou pas dans le brief existant.

### K. Score global du jour (subjectif)

| Dimension | Note /10 | Commentaire |
|---|---|---|
| Backend / DB | 9 | Solide, 16 migrations propres, RLS partout. Tide cache stub à finir ou drop. |
| Frontend code quality | 8 | 3 `any`, 0 `@ts-ignore`, 1 TODO. Très propre. |
| Tests | 6 | 9 fichiers ~42-116 tests, focus business logic. Manque E2E + RLS + Server Actions. |
| Marketing / Copy | 4 | Trop de promesses non tenues, 6 liens morts, témoignages fictifs, incohérences pricing. |
| SEO technique | 3 | `metadataBase` cassé ruine tout l'effort sprint 4 (sitemap, JSON-LD, OG dynamiques). |
| Sécurité | 7 | RLS active, secrets pas leakés, cron Bearer-protégé. Headers HTTP non vérifiés. Routes dev exposées. |
| Légal / Conformité | 3 | Mentions légales incomplètes publiquement — bloquant pré-Stripe. |
| CI/CD | 4 | Vercel auto-deploy + cron OK. Aucun workflow GitHub Actions. Lint skippé en build. |
| Documentation interne | 9 | CLAUDE.md riche, briefs sprint à jour, audit fondateur exhaustif, ce dossier audits/. |
| Roadmap / Velocity | 7 | 7 sprints livrés depuis le 11 mai = ~1 sprint / 1,5 jour de calendrier. Mais sprint 7.5 stagne 24h. |

**Score moyen : 6,0 / 10** — Le produit est solide techniquement, freiné par la couche marketing/SEO/légal. Un sprint 7.5 court et discipliné peut faire remonter à 7,5+ d'ici 1 semaine.

---

*Audit généré le 21/05/2026 à 02:47 Paris. Prochain audit prévu ce soir 20h.*
*Total : ~1000 lignes — premier passage du rituel quotidien.*
